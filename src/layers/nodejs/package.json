{
  "name": "lambda",
  "version": "1.0.0",
  "description": "Lambda Layer for packages such as: aws-xray-sdk-core. The main entry is not used.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-xray-sdk-core": "^3.4.1"
  }
}
